/**
 * The single store-owner account. The admin login page asks for a password
 * only; this address is used behind the scenes for authentication so the
 * owner never has to type or remember an email.
 */
export const ADMIN_EMAIL = "storeflora83@gmail.com";
